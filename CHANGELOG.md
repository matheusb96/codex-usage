# Changelog

## 0.1.0

- Initial release: `rcodex --usage` reports ChatGPT Plus / Codex rate-limit
  usage via the Codex App Server's `account/rateLimits/read`.
- `--json`, `--raw`, and `--simple` (sensors-style) output modes.
- Colorized default output, respecting `NO_COLOR` and `--color`/`--no-color`.
